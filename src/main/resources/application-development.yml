spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vibe
    username: vibe
    password: vibe
  batch:
    job:
      enabled: true

bernmobil:
  mappingrepository:
    datasource:
      url: jdbc:postgresql://localhost:5432/mappings
      username: vibe
      password: vibe